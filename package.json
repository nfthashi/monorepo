{
  "name": "nfthashi",
  "version": "0.2.0",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "prepare": "husky install",
    "ci": "run-p lint build test",
    "lint": "run-p fix:*",
    "lint:contracts": "yarn workspace contracts fix",
    "lint:web": "yarn workspace web fix",
    "fix": "run-p fix:*",
    "fix:contracts": "yarn workspace contracts fix",
    "fix:web": "yarn workspace web fix",
    "test": "run-p test:*",
    "test:contracts": "yarn workspace contracts test",
    "dev": "run-p dev:*",
    "dev:web": "yarn workspace web dev",
    "build": "run-p build:*",
    "build:contracts": "yarn workspace contracts build",
    "build:web": "yarn workspace web build"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.2.4",
    "@commitlint/config-conventional": "^16.2.4",
    "husky": "^8.0.1",
    "lint-staged": "^12.4.1",
    "npm-run-all": "^4.1.5"
  },
  "lint-staged": {
    "*.{ts,tsx}": "eslint --cache --fix",
    "*.--write": "prettier --write"
  }
}
